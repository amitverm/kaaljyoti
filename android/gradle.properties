org.gradle.jvmargs=-Xmx8G -XX:MaxMetaspaceSize=4G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true
# Use R8 in compatibility mode (not full mode). Full mode is AGP's most
# aggressive shrinking and is more likely to strip reflection/JNI classes from
# plugins despite their consumer rules. Compat mode is safer with our plugin set.
android.enableR8.fullMode=false
# This newDsl flag was added by the Flutter template
android.newDsl=false
# This builtInKotlin flag was added by the Flutter template
android.builtInKotlin=false
