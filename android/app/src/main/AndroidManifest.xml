<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Coarse location is enough: 1 min of sunrise ~ 18 km. Fine is
         requested only as the OS pairs them. -->
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/>
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>
    <!-- Kundli event alerts (flutter_local_notifications). Local only —
         nothing here talks to a server.
         POST_NOTIFICATIONS: Android 13+ runtime permission, requested
           the first time the user follows a kundli, never at launch.
         RECEIVE_BOOT_COMPLETED: lets the plugin re-arm pending alarms
           after a reboot; without it every scheduled alert is silently
           lost when the phone restarts.
         VIBRATE: default notification behaviour.
         NOTE the two exact-alarm permissions are deliberately ABSENT:
         alerts are scheduled inexact (see kundli_alert_service.dart), so
         SCHEDULE_EXACT_ALARM / USE_EXACT_ALARM — and the store review
         they attract — are not needed for a month-out dasha change. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED"/>
    <uses-permission android:name="android.permission.VIBRATE"/>
    <!-- Backup rules keep device-bound secrets (SQLCipher DB + Keystore-
         wrapped key material) out of Auto Backup — restoring them to a new
         device bricked the app (KAALJYOTI-PROD-E). -->
    <application
        android:label="Kaal Jyoti"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:fullBackupContent="@xml/backup_rules"
        android:dataExtractionRules="@xml/data_extraction_rules">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- Home-screen widgets: Daily Panchang and Live Transit,
             fed by the app via the home_widget plugin. -->
        <receiver
            android:name=".PanchangWidgetProvider"
            android:label="Daily Panchang"
            android:exported="true">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/widget_panchang_info" />
        </receiver>
        <receiver
            android:name=".TransitWidgetProvider"
            android:label="Live Transit"
            android:exported="true">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/widget_transit_info" />
        </receiver>
        <!-- flutter_local_notifications: the receiver that actually
             posts a scheduled kundli alert when its alarm fires, and
             the boot receiver that re-arms every pending alarm after a
             restart or an app update (Android drops alarms on both). -->
        <receiver
            android:exported="false"
            android:name="com.dexterous.flutterlocalnotifications.ScheduledNotificationReceiver" />
        <receiver
            android:exported="false"
            android:name="com.dexterous.flutterlocalnotifications.ScheduledNotificationBootReceiver">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED"/>
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED"/>
                <action android:name="android.intent.action.QUICKBOOT_POWERON" />
                <action android:name="com.htc.intent.action.QUICKBOOT_POWERON"/>
            </intent-filter>
        </receiver>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
